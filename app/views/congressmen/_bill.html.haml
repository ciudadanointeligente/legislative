%div.row
  %div.col-md-1
    = bill.uid
    = image_tag("statusicon/" + bill.icon, class:"hidden-xs")
  %div.col-md-11
    %h4
    = link_to bill.title, bill_path(bill.uid), :onclick => "ga('send','bill', '"+bill.uid+"')"
    %p
      = localize bill.creation_date.to_date
