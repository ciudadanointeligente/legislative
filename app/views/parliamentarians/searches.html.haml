= form_tag({controller: "parliamentarians", action: "searches"}, :method => 'get') do
  / buscador cabecera
  = render :partial => "simple_search_form"
  / fin buscador cabecera

  %div.row
    %div.col-md-4
      %h4.page-header
        Filtros

      = render :partial => "filter_search_form"
    %div.col-md-8
      %h4.page-header
        Resultado

      = render :partial => "box_parliamentarians_result" 