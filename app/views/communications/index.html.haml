%div.page-header
  %h2 Comunicación

%section#communication
  %div.panel.panel-default
    %div.panel-body
      = form_tag  :method => 'POST' do
        = field_set_tag do
          %div.row
            %div.col-md-6
              %div.form-group
                = text_field_tag :subject, nil ,:class => "form-control", :placeholder=>"Título de tu mensaje"
              %div.form-group
                = text_area_tag :content, nil ,:class => "form-control", :placeholder=>"Contenido del mensaje"
              %div.form-group
                = text_field_tag :author_name, nil ,:class => "form-control", :placeholder=>"Tu nombre"
              %div.form-group
                = text_field_tag :author_email, nil ,:class => "form-control",:placeholder=>"Tu email"
            %div.col-md-6
              %p
                Ingresa con qué candidatos quieres comunicarte:
              
              %div.tab-content
                %select.form-control.chosen{:multiple => "multiple", :name => "recipients[]"}
                  - @parliamentarians.persons.each do |parliamentarians|
                    %option{:value => parliamentarians.popit_api_uri}
                      =parliamentarians.name

        %div.text-right
        = submit_tag 'Enviar', :class => "btn btn-success"
%script{src: "http://cdnjs.cloudflare.com/ajax/libs/chosen/1.0/chosen.jquery.min.js"}
:javascript
  $(document).ready(function(){
    $(".chosen").chosen();
  })