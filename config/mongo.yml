defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: cabildo_abierto_development

test:
  <<: *defaults
  database: cabildo_abierto_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: cabildo_abierto
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
